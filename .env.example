# Database (PostgreSQL – e.g. Neon, Supabase, Railway)
DATABASE_URL="postgresql://user:password@localhost:5432/abtalks?schema=public"

# Auth – use long random strings in production
JWT_SECRET=
REFRESH_TOKEN_SECRET=

# App URL (for emails, links) – no trailing slash
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Email (e.g. Resend, SendGrid) – optional for dev
EMAIL_PROVIDER_KEY=
